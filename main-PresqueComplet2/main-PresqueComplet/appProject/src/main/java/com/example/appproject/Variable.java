package com.example.appproject;

public class Variable {
    //TODO : documentation

    //TODO : gerer toutes les variables

    //TODO : gerer instructions invalides

    //TODO : tester avec 3 classes filles

    //TODO : modifier les variables

    /*double value;

    String instruction;

    boolean condition;
    */
    String id;

    public Variable(String id) {
        this.id = id;}

    public String getVarId() {return this.id;}

    /*
    public com.example.appproject.Variable(double value) {
        //The value is omitted
        if(value == null)
            this.value = 0;

        else
            this.value = value;
    }

    public com.example.appproject.Variable(String instruction) {
        //The instruction is omitted
        if(instruction.equals(null))
            this.instruction = "";
        else
            this.instruction = instruction;
    }

    public com.example.appproject.Variable(boolean condition) {
        //The condition is omitted
        if(condition == null)
            this.condition = false;
        else
            this.condition = condition;
    }

    public double getVariable() {return this.value;}

    public void setVariable(double value) {this.value = value;}


    public String getVariable() {return this.instruction;}

    public void setVariable(String instruction) {this.instruction}

     */
}
